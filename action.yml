name: 'Docker Build Check'
description: 'Checks if a Docker build completes successfully'
inputs:
  path:
    description: 'Path to the Dockerfile'
    required: true
    default: '.'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.path }}
branding:
  icon: 'check-circle'
  color: 'blue'
